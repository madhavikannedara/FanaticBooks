import { ColorsetDirective } from './colorset.directive';

describe('ColorsetDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorsetDirective();
    expect(directive).toBeTruthy();
  });
});
